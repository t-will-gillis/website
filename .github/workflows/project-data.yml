name: Provide Project Column Data

on:
  project_column:
    types: [edited]

jobs:
  Tell-Me-Project:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The project card event is called ${{ github.event_name }}"
  Tell-Me-Project2:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The project card event is called ${{ github.project_card }}"
