name: Pull Request Trigger
on:
  pull_request:
    types: [closed]
    branches:
      - 'gh-pages'
      - 'feature-homepage-launch'
      - 'remove-merged-prs-project-board-2592'

jobs:
  Hello-World:
    runs-on: ubuntu-latest
    steps:
     - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
     
  Hey-I-Am-Merged:
    runs-on: ubuntu-latest
    if: ${{ github.event_name == 'pull_request' && github.event.action == 'merged'}}
    steps:
     - run: echo "I am running beccause ***github.event_name == 'pull_request' && github.event.action == 'merged'***."
  
  Truncated-merge-message:
    runs-on: ubuntu-latest
    if: ${{ github.event.action == 'merged'}}
    steps:
     - run: echo "I am running beceause only --> github.event.action == 'merged' <--."
     
#   Remove-Merged-PRs-From-Project-Board:
#     runs-on: ubuntu-latest
#     # Trigger this action if pull request is merged
#     if: ${{ github.event_name == 'pull_request' && github.event.action == 'merged'}}
#     steps: 
#       - uses: actions/checkout@v2
#       - uses: actions/github-script@v4
