name: Pull Request Trigger
on:
  pull_request:
    types: [closed]
    branches:
      - 'gh-pages'
      - 'feature-homepage-launch'
      - 'test_project_board_for_gha'

jobs:
  Hello-World:
    runs-on: ubuntu-latest
    steps:
     - run: echo "🎉 This 'Pull Request Trigger' job was automatically triggered by a ${{ github.event_name }} event."
     
  Delete-Merged-PRs:
    runs-on: ubuntu-latest
    if: ${{ github.event.action == 'merged'}} 
    steps: 
      - name: Delete Merged PRs
      - uses: alex-page/github-project-automation-plus@v0.8.1
        with:
          project: Project Board
          repo-token: ${{ secrets.GHPROJECT_TOKEN }}
          action: delete
          
      - name: Echo Delete Attempt
      - run: echo "This job's status is ${{ job.status }}."
